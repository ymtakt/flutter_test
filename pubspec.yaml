name: dart_flutter_practice
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

workspace:
  - packages/freezed_normal_object
  - packages/my_lints

dependencies:
  flutter:
    sdk: flutter
  hooks_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  flutter_hooks: ^0.21.2
  freezed_annotation: ^3.0.0 # freezedのアノテーション

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  freezed: ^3.0.3  # freezedのコード生成
  build_runner: ^2.4.14  # コード生成のために必要
  json_serializable: ^6.9.4  # JSONシリアライズ用（freezedでよく使用）
  riverpod_generator: ^2.6.5

flutter:
  uses-material-design: true
